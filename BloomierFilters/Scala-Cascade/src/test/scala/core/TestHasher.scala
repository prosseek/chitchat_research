package core

import org.scalatest._

/**
 * Created by smcho on 7/1/14.
 */
class TestHasher extends FunSuite {
  test ("Simple test") {
//    val h = Hasher(maxVal = 10)
//    assert(h.get("Hello", 3).size == 3)
//
//    for (i <- Range(1, 100)) {
//      assert(Hasher.get("Hello", 200, i).size == i)
//    }
//    for (i <- Range(1, 100)) {
//      // all the values generated are below 10
//      assert(Hasher.get("Hello", 200, i).forall(i => i < 200))
//    }

  }
}
